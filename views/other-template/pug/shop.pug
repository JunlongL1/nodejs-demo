extends layouts/main-layout.pug

block styles
  link(rel="stylesheet", href="/css/product.css")


block content
  main
    h1 My Products
    if prods.length > 0
      each product in prods 
        p #{product.title}
    else 
      p Empty!
